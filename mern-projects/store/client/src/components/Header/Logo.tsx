import { Box, Typography, Link } from '@mui/material';

const Logo: React.FC = () => {
  return (
    <Box>
      <Typography  variant="h5">
        Store
      </Typography>
    </Box>
  );
};

export default Logo;
